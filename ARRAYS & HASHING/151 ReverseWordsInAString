class Solution:
    def reverseWords(self, s: str) -> str:
        word_list = s.split()
        str = ""
        for i in range (len(word_list)-1,0,-1):
            str+= word_list[i] + " "
        str+= word_list [0]
        return str

